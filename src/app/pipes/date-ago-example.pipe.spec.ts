import { DateAgoExamplePipe } from './date-ago-example.pipe';

describe('DateAgoExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new DateAgoExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
